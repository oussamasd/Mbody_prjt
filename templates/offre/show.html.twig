{% extends 'base.back.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1 style="color: #d5606e">Afficher une offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Delai</th>
                <td>{{ offre.delai ? offre.delai|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.Description }}</td>
            </tr>
            <tr>
                <th>Nom_Abonnement</th>
                <td>{{ offre.getNomAbonnement.getNom()}}</td>            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_index') }}">retourner à la liste</a>

{#    <a href="{{ path('offre_edit', {'id': offre.id}) }}">editer</a>#}

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
