{% extends 'base.back.html.twig' %}

{% block title %}New Categories{% endblock %}

{% block body %}
    <h1>Create new Categories</h1>

    {{ include('categories/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('categories_index') }}">back to list</a>
{% endblock %}
